# claimsense-ai
ClaimSense AI ingests feedback from multiple simulated channels, automatically classifies it by sentiment and issue category, clusters similar complaints using embeddings so a manager can see, &amp; drafts a context-aware, policy-compliant response for the servicing agent to review.
